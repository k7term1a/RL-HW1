# RL-HW1

馬可夫決策過程 (MDP) 的「行動-評估」互動式展示專案。

## 功能

- 輸入 `n` (`3` 到 `10`) 建立 `n x n` 網格
- 設定起點、終點與障礙物（上限 `n-2`）
- 隨機生成每格行動箭頭
- 顯示每格評估分數與四向 (`U/D/L/R`) 行動分數
- 依策略顯示由起點出發到終點的路徑與步數

## 本機執行

```bash
pip install -r requirements.txt
python app.py
```

啟動後開啟 `http://127.0.0.1:5000`。
